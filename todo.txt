Todo:
- Tasks:
   > Store tasks in storage: All, current, completed
   > Task select picks a task from the list
   > Once task is finished, remove from the list and move to completed
   > Functions Needed:
     - Initialize task storage (All tasks, number of tasks, no current, no special)
     - Update all tasks

- Spectators:
   > Life Timer should show in purple and say "Spectators Have No Time"
   > Spectators should have access to a command to place themself in spectator / survival mode

- Admin:
   > Command to reroll the current task without removal (Does not count for session reroll)
   > Command to toggle dev mode (Show command block output)
   > Command to show admin commands (/trigger admin_help)

- Gamerules:
   > Make sure keep inventory is on
   > Make sure command block output is off

- Setup:
   > Put all player names in the users.txt
   > Put all tasks in the tasks.txt
   > Run a clean compile before game start

````````````````````````````

Task randomization
- Very small chance to reroll specific tasks just due to nature of coding

Spectators:
- Can not initiate pvp, be fought, or set traps
- You can only interact socially with active players (No helping with tasks, giving items, taking items, etc)
- Will have a command to place yourself in and out of spectator mode

Todo:
- Add a free skip admin command
- Stop missions from selecting ones we've done