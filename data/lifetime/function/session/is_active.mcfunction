##
## Return if the session is active
##
## Return
## > 1 If the session is active
## > 0 if the session is not active

# This is just stored in #session SessionActive
return run scoreboard players get #session SessionActive